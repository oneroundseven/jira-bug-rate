<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title><%=title%></title>
    <link rel="stylesheet" type="text/css" href="/pages/home.css" />
    <link rel="stylesheet" type="text/css" href="/fonts/foundation-icons.css" />
</head>
<body>

<header>
    <div class="title-bar">
        <div class="title-bar-left">
            <a class="title-bar-title bugs-log" href="/">BUG <strong>Rate</strong></a>
        </div>
        <div class="title-bar-right">
            <form action="/search" method="get">
                <ul class="menu float-right">
                    <li><input name="version" required type="search" placeholder="Search" <% if (typeof hisSearch !== 'undefined') { %> value="<%=hisSearch%>" <% } %> /></li>
                    <li><button type="submit" class="button">Search</button></li>
                </ul>
            </form>
        </div>
    </div>
</header>
<% if (allTask && allTask.length > 0) { %>
<% allTask.forEach(function(task, index) { %>
<div class="content-wrap">
    <h3 class="version-text"><%=task['fixVersion']%></h3>
    <div class="version-timeline">
        <div class="m-step step-5">
            <div class="step-item step-ed">
                <span class="dot"></span>
                <i class="line"></i>
                <div class="txt-box">
                    <div class="time-start"><% if (task['devStartTime']) { %><%=task['devStartTime']%><% } else { %>-<% } %></div>
                    <div class="time-desc">Start Dev</div>
                </div>
            </div>
            <div class="step-item step-ed">
                <span class="dot"></span>
                <i class="line"></i>
                <div class="txt-box">
                    <div class="time-start"><% if (task['releaseTestTime']) { %><%=task['releaseTestTime']%><% } else { %>-<% } %></div>
                    <div class="time-desc">Start Testing</div>
                </div>
            </div>
            <div class="step-item step-ed">
                <span class="dot"></span>
                <i class="line"></i>
                <div class="txt-box">
                    <div class="time-start"><% if (task['releasePTime']) { %><%=task['releasePTime']%><% } else { %>-<% } %></div>
                    <div class="time-desc">Release -P</div>
                </div>
            </div>
            <div class="step-item step-ed">
                <span class="dot"></span>
                <i class="line"></i>
                <div class="txt-box">
                    <div class="time-start">-</div>
                    <div class="time-desc">Customer Accepting</div>
                </div>
            </div>
            <div class="step-item step-ed">
                <span class="dot"></span>
                <div class="txt-box">
                    <div class="time-start"><% if (task['releaseTime']) { %><%=task['releaseTime']%><% } else { %>-<% } %></div>
                    <div class="time-desc">Release Product</div>
                </div>
            </div>
        </div>
    </div>
    <div class="version-bugs">
        <table class="unstriped">
            <thead>
            <tr>
                <th width="150">FE Developer</th>
                <th>Dev Time</th>
                <th>Test Bugs</th>
                <th>Release P Bugs</th>
                <th>Req Update</th>
                <th width="400">Related Tasks</th>
            </tr>
            </thead>
            <tbody>
                <% var testBugs = 0; var pBugs = 0; var reqUpdate = 0; var workTime = 0; %>
                <% task['users'].forEach(function(user, index) { %>
                <tr>
                    <td><%=user['assignee']%><br>
                        <% if (user['testBugsRate'] > 0) { %>
                            <%
                                var starCount = 0;
                                if (user['testBugsRate'] < 2.46) {
                                    starCount = 1;
                                }

                                if (user['testBugsRate'] < 1.5) {
                                    starCount = 2;
                                }

                                if (user['testBugsRate'] < 1) {
                                    starCount = 4;
                                }

                                if (user['testBugsRate'] < 0.5) {
                                    starCount = 7;
                                }
                            %>
                            <% for (var i = 0; i < starCount; i++ ) { %>
                                <i class="fi-star"></i>
                            <% } %>
                            <% if (starCount === 0) { %>
                                <i class="fi-skull"></i>
                            <% } %>
                        <% } %>
                    </td>
                    <td>
                        <% if (user['devTime']) { %>
                        <%=user['devTime']%> h<br>
                        <%=(user['devTime']/7.5).toFixed(2)%> days
                        <% workTime += user['devTime'] %>
                        <% } else { %>-<% } %>
                    </td>
                    <td>
                        <% if (user['tbugs']) { %>
                            <span class="bugs"><%=user['tbugs']%></span>
                            <% if (user['testBugsRate']) { %>
                                <span class="label <% if (user['testBugsRate'] < 1.5) { %>success<% } %>
                                    <% if (user['testBugsRate'] >= 1.5 && user['testBugsRate'] < 2.46) { %>warning<% } %>
                                    <% if (user['testBugsRate'] >= 2.46) { %>alert<% } %>
                                    ">
                                <%=user['testBugsRate']%>/day</span>
                            <% } %>
                            <% testBugs += user['tbugs'] %>
                        <% } else { %>-<% } %>
                    </td>
                    <td>
                        <% if (user['pbugs']) { %>
                            <span class="bugs"><%=user['pbugs']%></span>
                            <% if (user['PBugsRate']) { %>
                                <span class="label <% if (user['PBugsRate'] < 1) { %>success<% } %>
                                    <% if (user['PBugsRate'] >= 1) { %>alert<% } %>
                                    ">
                                <%=user['PBugsRate']%>/day
                                </span>
                            <% } %>
                            <% pBugs += user['pbugs'] %>
                        <% } else { %>-<% } %>
                    </td>
                    <td>
                        <% if (user['reqUpdated']) { %>
                            <span class="req-update"><%=user['reqUpdated']%></span>
                            <% reqUpdate += user['reqUpdated'] %>
                        <% } else { %>-<% } %>
                    </td>
                    <td>
                        <ul>
                        <% user['tasks'].forEach(function(subTask, index) { %>
                            <li><a href="<%=subTask['link']%>" target="_blank"><%=subTask['taskName']%></a></li>
                        <% }) %>
                        </ul>
                    </td>
                </tr>
                <% }) %>
                <tr>
                    <td>总计</td>
                    <td><%=workTime%> h<br><%=(workTime/7.5).toFixed(2)%> days</td>
                    <td><%=testBugs%></td>
                    <td><%=pBugs%></td>
                    <td><%=reqUpdate%></td>
                    <td>-</td>
                </tr>
            </tbody>
        </table>
    </div>
</div>
<% if (allTask.length - 1 !== index) { %>
    <hr>
<% } %>
<% }) %>
<% } else { %>
<div class="content-wrap">
    <div class="no-result">
        没有任何结果
    </div>
</div>
<% } %>
<script type="text/javascript" charset="utf-8" src="/js/libs/jquery.js"></script>
<script type="text/javascript" charset="utf-8" src="/pages/home.js"></script>
</body>
</html>